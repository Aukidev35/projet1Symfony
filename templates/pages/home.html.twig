{% extends "base.html.twig" %}
{% block title %}Projet . Accueil{% endblock %}
{% block body %}

<div class="jumbotron">
    <div class="container mt-4">
        <h1 class="display-4 mt-4">Bienvenue sur mon projet</h1>
        <p class="lead">Mon projet est une application qui va te permettre de créer des recettes à base d'ingrédient que tu auras
                        toi-même créer; Tu pourras partager tes recettes à la communauté du site, ou bien les garder ne privées.</p>
        <hr class="my-4">
        <p>pour commencer, rendez-vous sur la pages d'inscription pour utiliser l'application.</p>
        <a class="btn btn-primary btn-lg" href="#" role="button">Inscription</a>

        <div class="recipes">
            <h3>Recettes de la communauté</h3>
            <div class="d-flex justify-content-center flex-wrap">
                {% for recipe in recipes %}
                    <div class="card text-white bg-primary mb-3 mr-3" style="max-width: 20rem;">
                        <div class="card-header">Recette numéro{{ recipe.id}}</div>
                        <div class="card-body">
                            <h4 class="card-title">{{ recipe.name}}</h4>
                            <p class="card-text">{{ recipe.desciption|slice(0, 100) ~ '...'}}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
</div>

{% endblock %}
